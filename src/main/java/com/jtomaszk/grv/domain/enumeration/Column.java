package com.jtomaszk.grv.domain.enumeration;

/**
 * The Column enumeration.
 */
public enum Column {
    FIRST_NAME, LAST_NAME, ANOTHER_LAST_NAME, START_DATE, END_DATE, VALID_TO, EXTERNAL_ID, BOX_EXTERNAL_ID, INFO, DOC_NR
}
